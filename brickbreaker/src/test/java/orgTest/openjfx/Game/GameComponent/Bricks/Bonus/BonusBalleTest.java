/*package orgTest.openjfx.Game.GameComponent.Bricks.Bonus;

import static org.mockito.Mockito.*;
import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.openjfx.Game.GameClass.Level;
import org.openjfx.Game.GameComponent.Bricks.Bonus.Bonus_balle;

public class BonusBalleTest {

    private Level levelMock;

    @BeforeEach
    void setUp() {
        // Création du mock pour l'objet Level
        levelMock = mock(Level.class);
    }

    // Test de la méthode apply()
    @Test
    void testApply() {
        // Appeler la méthode apply() de Bonus_balle
        Bonus_balle.apply(levelMock);

        // Vérifier que la méthode addBall() a été appelée une fois sur le niveau
        verify(levelMock, times(1)).addBall();
    }
}*/
