/*package orgTest.openjfx.Game.GameComponent.Bricks.Bonus;
import static org.mockito.Mockito.*;
import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.openjfx.Game.GameClass.ScoreboardController;
import org.openjfx.Game.GameComponent.Bricks.Bonus.Bonus_vie;

public class BonusVieTest {

    private ScoreboardController scoreboardControllerMock;

    @BeforeEach
    void setUp() {
        // Création du mock de ScoreboardController avant chaque test
        scoreboardControllerMock = mock(ScoreboardController.class);
    }

    // Test de la méthode apply() de la classe Bonus_vie
    @Test
    void testApply() {
        // Appel de la méthode apply() de Bonus_vie
        Bonus_vie.apply(scoreboardControllerMock);

        // Vérification que la méthode addLife() de ScoreboardController a été appelée une fois
        verify(scoreboardControllerMock, times(1)).addLife();
    }
}*/

